{
  "name": "lowcortisol-platform",
  "version": "1.0.0",
  "description": "Fake API for LowCortisol Platform",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json",
    "dev": "json-server --host 0.0.0.0 --port 3000 db.json"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "engines": {
    "node": ">=20"
  }
}
